---
interface Props {
  lang?: 'en' | 'ja'
}
const { lang = 'ja' } = Astro.props;

interface Link {
  type: string;
  caption: string;
  url: string;
}

interface Props {
  id: number;
  name: string;
  sub: string;
  address: string;
  link: Link[];
  description: string;
  pic: string;
}

const { id, name, sub, address, link, description, pic } = Astro.props;
---

<div class="c-business-item" data-item={id}>
  <figure class="c-business-item__pic">
    <img src={`/common/images/about/${pic}`} alt={name}>
  </figure>
  <div class="c-business-item__info">
    <h3 class="c-business-item__name">{name}</h3>
    <span class="c-business-item__sub">{sub}</span>
    <div class="c-business-item__description" data-lang={lang}>
      <p set:html={description}></p>
    </div>
    <p class="c-business-item__address" set:html={address} data-lang={lang}></p>
    <div class="c-business-item__link">
      {link.map((linkItem) => (
        linkItem.type === 'email' ? (
          <a href={`mailto:${linkItem.url}`}>{linkItem.caption}</a>
        ) : (
          <a href={linkItem.url} target="_blank" rel="noopener noreferrer">{linkItem.caption}</a>
        )
      ))}
    </div>
  </div>
</div>

<style lang="scss" is:global>
@use '../../../assets/scss/mediaquery' as me;
@use '../../../assets/scss/mixin' as mi;

.c-business-item {
  @include me.pc {
    display: flex;
    flex-direction: row-reverse;
    justify-content: space-between;
    align-items: flex-start;
    column-gap: 40px;
    padding-block: 40px;
  }
  @include me.sp {
    padding-block-end: 60px;
  }

  &__pic {
    @include me.pc {
      max-width: 557px;
    }
  }

  &__info {
    display: grid;
    @include me.pc {
      row-gap: 16px;
      max-width: 560px;
    }
    @include me.sp {
      margin-block-start: 15px;
    }
  }

  &__name {
    font-size: 3rem;
    font-weight: 700;
    line-height: 0.96;
    letter-spacing: -.02rem;
    @include me.pc {
      grid-row: 1;
    }
    @include me.sp {
      font-size: 1.8rem;
      line-height: 1.6;
    }
  }

  &__sub {
    margin-block-start: -8px;
    color: var(--c-gray);
    font-size: 1.4rem;
    line-height: 2.2;
    @include me.sp {
      margin-block-start: 2px;
    }
  }

  &__description {
    display: grid;
    row-gap: 16px;
    font-size: 1.4rem;
    line-height: 1.8;
    @include me.sp {
      margin-block-start: 24px;
    }
    &[data-lang="en"] {
      font-family: var(--f-en);
    }

    p {

    }
  }

  &__address {
    color: var(--c-gray);
    font-size: 1.4rem;
    line-height: 1.6;
    @include me.sp {
      margin-block-start: 24px;
    }
    a {
      color: var(--c-gray);
      text-decoration: underline;
    }
  }

  &__link {
    font-size: 1.4rem;
    line-height: 0.6363;
    @include me.sp {
      margin-block-start: 8px;
      line-height: 1.8;
    }

    a {
      display: inline-block;
      position: relative;
      color: var(--c-white);
      &::after {
        content: "";
        position: absolute;
        bottom: -2px;
        left: 0;
        width: 100%;
        height: 1px;
        background-color: var(--c-white);
        @include me.sp {
          bottom: 4px;
        }
      }
    }
  }
}
</style>
